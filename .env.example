# TaxiConnect Cloudflare configuration
# Runtime configuration is managed by Cloudflare Wrangler.
# Add local secrets only when required by the Worker; never commit secrets.
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_API_TOKEN=your_api_token
