{
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && firebase deploy --only database,hosting --project taxiconnect-dev"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
